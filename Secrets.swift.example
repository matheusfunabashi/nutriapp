// Template for the git-ignored Sage/Secrets.swift.
// Copy this file to Sage/Secrets.swift and fill in the real values
// (ask a teammate — they are not committed anywhere in this repo).
// It lives at the repo root, outside the synced Sage/ group, so Xcode
// never tries to compile the template itself.

import Foundation

enum Secrets {
    /// Shared secret the backend expects in the `X-Sage-Key` header.
    static let sageAPIKey = "<paste the SAGE_API_KEY here>"
}
